<%= form_for @reviews do |f| %>
    <%= f.collection_select :park_id, Park.all, :id, :name %>
    <%= f.text_area :content %>
    <%= f.number_field :rating %>
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.submit %>
  <% end %>
  